import React from "react";

export default function ViewThree() {
  return (
    <div>
      <h1>Page 3</h1>
      <h2>
        The selector is approximately equivalent to the mapStateToProps argument
        to connect conceptually. The selector will be called with the entire
        Redux store state as its only argument.
      </h2>
    </div>
  );
}
